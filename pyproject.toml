[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ephemeralkey"
version = "3.0.0"
description = "Suite de Privacidad y Generacion de Credenciales"
